import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    input: {
        height: 40,
        width: '100%',
        backgroundColor: '#ededed',
        marginBottom: 16,
        borderRadius: 4,
        paddingLeft: 40
    }
});